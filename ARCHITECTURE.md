# 🏗️ Архитектурный обзор Obsidian Telegram Bot

> **Версия:** 2.0  
> **Статус:** Production Ready

---

## 📑 Содержание

1. [Общая архитектура](#общая-архитектура)
2. [Компоненты системы](#компоненты-системы)
3. [Модульная структура](#модульная-структура)
4. [Потоки данных](#потоки-данных)
5. [Варианты развертывания](#варианты-развертывания)
6. [Безопасность](#безопасность)

---

## 🎯 Общая архитектура

### Высокоуровневая схема

```
┌──────────────┐
│   Telegram   │
│   (Client)   │
└──────┬───────┘
       │ HTTP/HTTPS (Bot API)
       │
┌──────▼────────────────────────┐
│   Obsidian Telegram Bot       │
│   ┌──────────────────────┐    │
│   │  bot.py              │    │
│   │  - Handlers          │    │
│   │  - Rate limiting     │    │
│   └──────┬───────────────┘    │
│          │                    │
│   ┌──────▼───────────────┐    │
│   │  llm_processor.py    │    │
│   │  - Smart Processing  │    │
│   └──────┬───────────────┘    │
│          │                    │
│   ┌──────▼───────────────┐    │
│   │  github_handler.py   │    │
│   │  - GitHub API        │    │
│   └──────────────────────┘    │
└───────────────────────────────┘
       │                    │
       │ GitHub API         │ OpenAI API
       ▼                    ▼
┌──────────────┐    ┌──────────────┐
│    GitHub    │    │   OpenAI     │
│  Repository  │    │   (GPT/Whisper) │
└──────┬───────┘    └──────────────┘
       │ Git Pull
       ▼
┌──────────────┐
│   Obsidian   │
│   (Local)    │
└──────────────┘
```

### Архитектурные принципы

- **Асинхронность**: aiogram 3.x с asyncio для неблокирующих операций
- **Модульность**: четкое разделение ответственности между модулями
- **Stateless**: бот не хранит состояние между перезапусками
- **Event-driven**: обработка событий от Telegram через polling

---

## 🧩 Компоненты системы

### 1. Telegram Bot (bot.py)

**Роль:** Интерфейс взаимодействия с пользователем

**Основные функции:**
- Прием текстовых и голосовых сообщений
- Обработка команд (`/start`, `/help`)
- Авторизация по User ID
- Rate limiting (10 голосовых/час, 20 LLM/час)
- Интеграция с LLM и GitHub

### 2. LLM Processor (llm_processor.py)

**Роль:** Обработка заметок через AI

**Основные функции:**
- Извлечение тегов (2-5 штук, адаптивно)
- Генерация резюме (1-2 предложения)
- Извлечение задач с датами, временем и приоритетами
- Retry механизм (3 попытки с экспоненциальной задержкой)

**Модель:** GPT-4o-mini (по умолчанию)

### 3. GitHub Handler (github_handler.py)

**Роль:** Управление заметками в GitHub репозитории

**Основные функции:**
- Создание/обновление дневных файлов (`YYYY-MM-DD.md`)
- Формирование YAML frontmatter с метаданными
- Append-only операции (безопасное добавление)
- Обработка ошибок GitHub API

### 4. Interactive Handler (interactive_handler.py)

**Роль:** UI для редактирования обработанных заметок

**Основные функции:**
- Отображение превью с inline кнопками
- Редактирование тегов, резюме, задач
- Регенерация через LLM
- Управление сессиями (10 минут timeout)

### 5. Utils (utils.py)

**Роль:** Вспомогательные функции

**Основные функции:**
- `extract_priority()` - определение приоритета из текста
- `normalize_date_for_obsidian()` - конвертация дат в формат Obsidian

---

## 📁 Модульная структура

```
bot.py (542 строки)
├── Константы и инициализация
├── Rate limiting функции
│   ├── check_voice_rate_limit()
│   └── check_llm_rate_limit()
├── Обработчики команд
│   ├── cmd_start()
│   └── cmd_help()
├── Обработчики сообщений
│   ├── handle_voice_message()
│   │   ├── Проверка лимитов
│   │   ├── Скачивание аудио
│   │   ├── Транскрибация (Whisper)
│   │   └── Smart Processing (опционально)
│   └── handle_text_message()
│       └── Smart Processing (опционально)
└── main() - запуск бота

github_handler.py (230 строк, после рефакторинга)
├── GitHubHandler (класс)
│   ├── connect_to_repo()
│   ├── _ensure_inbox_exists()
│   ├── _format_processed_note()
│   ├── _create_or_append_note() [НОВОЕ: общая логика]
│   ├── create_note() [обертка]
│   └── create_voice_note() [обертка]

llm_processor.py (556 строк)
├── SYSTEM_PROMPT (детальные инструкции для LLM)
├── ActionItem (dataclass) - структура задачи
├── ProcessingResult (dataclass) - результат обработки
├── process_text() - основная функция
├── _call_llm_with_retry() - вызов с повторами
└── Вспомогательные функции парсинга и валидации

interactive_handler.py (393 строки)
├── ProcessingSession (dataclass)
├── InteractiveHandler (класс)
│   ├── show_processing_preview()
│   ├── handle_callback()
│   └── Обработчики действий (approve, edit, regenerate, delete)
```

---

## 🔄 Потоки данных

### Обработка текстового сообщения

```
User → Telegram → Bot
                  ↓
            Authorization ✓
                  ↓
         Smart Processing? (если включено)
         ↓YES              ↓NO
    LLM Processing    Direct Save
         ↓                 ↓
    Interactive Preview   ↓
         ↓                 ↓
    User Approval         ↓
         ↓________________↓
                  ↓
         GitHub Handler
                  ↓
         GitHub Repository
                  ↓
         Obsidian Git (pull)
                  ↓
         Obsidian Vault
```

### Обработка голосового сообщения

```
User → Telegram → Bot
                  ↓
            Authorization ✓
                  ↓
            Rate Limit ✓ (10/час)
                  ↓
         Download Audio (.ogg)
                  ↓
    Whisper API Transcription
         (3 retries)
                  ↓
         Smart Processing? (если включено)
         ↓YES              ↓NO
    LLM Processing    Direct Save
         ↓                 ↓
    Interactive Preview   ↓
         ↓                 ↓
         [остальное как для текста]
```

---

## 🚀 Варианты развертывания

### 1. Docker (рекомендуется)

```yaml
# docker-compose.yml
services:
  obsidian-bot:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./logs:/app/logs
```

**Плюсы:**
- 24/7 работа
- Автоматический перезапуск
- Изоляция
- Ротация логов

**Команды:**
```bash
docker-compose up -d --build  # Запуск
docker-compose logs -f        # Логи
docker-compose restart        # Перезапуск
```

### 2. Systemd Service (Linux)

**Плюсы:**
- Нативная интеграция с ОС
- Автозапуск при загрузке
- Системные логи

**Команды:**
```bash
sudo systemctl start obsidian-bot
sudo systemctl status obsidian-bot
journalctl -u obsidian-bot -f
```

### 3. Screen/tmux (простой)

**Плюсы:**
- Не требует Docker
- Легкий старт

**Минусы:**
- Нет автоперезапуска
- Ручное управление

---

## 🔒 Безопасность

### Реализованные меры

1. **Авторизация**
   ```python
   # Только ALLOWED_USER_ID может использовать бота
   def is_authorized(user_id: int) -> bool:
       return user_id == config.ALLOWED_USER_ID
   ```

2. **Защита токенов**
   - Переменные окружения через `.env`
   - `.gitignore` исключает `.env` из Git
   - `.env.example` как шаблон

3. **Rate Limiting**
   - Голосовые: 10 сообщений/час
   - LLM: 20 запросов/час
   - Защита от превышения квот API

4. **Валидация входных данных**
   - Длительность голосового ≤ 600 секунд
   - Размер файла ≤ 25 МБ
   - Проверка наличия обязательных env переменных

5. **GitHub Access**
   - Токен с минимальными правами (`repo`)
   - Доступ только к указанному репозиторию

### Рекомендации

- Используйте webhook вместо polling для production
- Настройте firewall на VPS (только SSH порт 22)
- Регулярно обновляйте зависимости
- Мониторьте логи на предмет подозрительной активности

---

## 📊 Ограничения

### Лимиты API

| API | Лимит | Цена |
|-----|-------|------|
| Telegram Bot API | 30 сообщений/сек | Бесплатно |
| OpenAI Whisper | 25 МБ/файл | $0.006/минута |
| OpenAI GPT-4o-mini | RPM зависит от тарифа | $0.150 input / $0.600 output за 1M токенов |
| GitHub API | 5,000 запросов/час | Бесплатно |

### Встроенные ограничения

```python
MAX_VOICE_DURATION = 600       # 10 минут
MAX_FILE_SIZE = 25 * 1024 * 1024  # 25 МБ
MAX_VOICE_PER_HOUR = 10        # голосовых/час
MAX_LLM_REQUESTS_PER_HOUR = 20 # LLM запросов/час
```

---

## 🎯 Итоги

### Сильные стороны

✅ Простая и понятная архитектура  
✅ Минимум зависимостей (4 пакета)  
✅ Хорошая модульность  
✅ Retry механизмы для API  
✅ Обработка ошибок  

### Области для улучшения

⚠️ Rate limiting хранится в памяти (сбрасывается при перезапуске)  
⚠️ Нет постоянного хранения состояния  
⚠️ Ограничен одним пользователем (по дизайну)

### Масштабирование

Для multi-user версии потребуется:
- База данных (PostgreSQL)
- Persistent rate limiting (Redis)
- Очереди для обработки (RabbitMQ)
- Управление пользователями и подписками

---

## 📞 Ссылки

- **README.md** - основная документация
- **SMART_PROCESSING.md** - подробности о Smart Processing
- **DEPLOY_VPS.md** - инструкция по деплою
- **CHEATSHEET.md** - справка команд

---

**Версия документа:** 2.0 (сокращенная)  
**Последнее обновление:** 17.02.2026  
**Автор:** mkoval-pwork
