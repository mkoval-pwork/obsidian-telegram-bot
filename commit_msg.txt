feat: Add automatic deployment via GitHub Actions

Auto-deploy setup:
- Created GitHub Actions workflow (.github/workflows/deploy.yml)
- Automatic deployment on push to main branch
- SSH connection to VPS for updates
- Docker container restart after pull

Additional files:
- update.sh: Quick manual update script for VPS
- AUTO_DEPLOY_SETUP.md: Detailed setup instructions

Benefits:
- No need to SSH into VPS manually
- Bot updates automatically on git push
- Deployment logs visible in GitHub Actions
- Can rollback to previous version easily

Setup required:
1. Add SSH key to VPS
2. Configure GitHub Secrets (VPS_HOST, VPS_USERNAME, VPS_SSH_KEY, VPS_PORT)
3. Push to main - bot updates automatically

See AUTO_DEPLOY_SETUP.md for full instructions
