# 🚀 Настройка автоматического деплоя

Автоматический деплой через GitHub Actions - при каждом `git push` на `main` бот обновляется на VPS автоматически.

---

## ⚡ Что это даёт?

- ✅ Push изменения → бот автоматически обновляется на VPS
- ✅ Не нужно заходить в SSH и делать git pull вручную
- ✅ Логи деплоя видны в GitHub Actions
- ✅ Можно откатиться на предыдущую версию одной кнопкой

---

## 📋 Шаг 1: Генерация SSH ключа на VPS

Подключитесь к VPS через SSH и выполните:

```bash
# Создать SSH ключ (если еще не создан)
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_deploy -N ""

# Добавить ключ в authorized_keys
cat ~/.ssh/github_deploy.pub >> ~/.ssh/authorized_keys

# Скопировать ПРИВАТНЫЙ ключ (нужно будет вставить в GitHub)
cat ~/.ssh/github_deploy
```

Скопируйте весь вывод последней команды (от `-----BEGIN` до `-----END`).

---

## 🔐 Шаг 2: Добавление секретов в GitHub

1. Откройте ваш репозиторий на GitHub:
   ```
   https://github.com/mkoval-pwork/obsidian-telegram-bot
   ```

2. Перейдите в **Settings** → **Secrets and variables** → **Actions**

3. Нажмите **New repository secret** и добавьте 4 секрета:

### VPS_HOST
- Name: `VPS_HOST`
- Value: IP адрес вашего VPS (например: `123.45.67.89`)

### VPS_USERNAME
- Name: `VPS_USERNAME`
- Value: `root` (или другой пользователь, под которым работает бот)

### VPS_SSH_KEY
- Name: `VPS_SSH_KEY`
- Value: Приватный SSH ключ из Шага 1 (весь текст от `-----BEGIN` до `-----END`)

### VPS_PORT
- Name: `VPS_PORT`
- Value: `22` (стандартный SSH порт, если не меняли)

---

## ✅ Шаг 3: Проверка работы

### Автоматический деплой:

1. Сделайте любое изменение в коде
2. Закоммитьте и запушьте на `main`:
   ```bash
   git add .
   git commit -m "test: Auto-deploy test"
   git push
   ```

3. Откройте вкладку **Actions** в GitHub:
   ```
   https://github.com/mkoval-pwork/obsidian-telegram-bot/actions
   ```

4. Вы увидите запущенный workflow "Deploy to VPS"

5. Дождитесь выполнения (обычно 20-40 секунд)

6. Проверьте бота в Telegram - он должен работать с новым кодом

### Ручной деплой через GitHub:

1. Перейдите в **Actions** → **Deploy to VPS**
2. Нажмите **Run workflow** → **Run workflow**
3. GitHub выполнит деплой вручную

---

## 🛠️ Альтернатива: Ручное обновление на VPS

Если автодеплой не нужен, можно обновлять бота вручную через простой скрипт:

```bash
# На VPS выполните:
cd /root/obsidian-telegram-bot
chmod +x update.sh
./update.sh
```

Скрипт `update.sh` автоматически:
- Подтянет изменения из GitHub
- Перезапустит бота (Docker или Screen)
- Покажет логи

---

## 🔍 Проверка логов после деплоя

### Через GitHub Actions:
- Откройте вкладку **Actions**
- Кликните на последний workflow
- Раскройте шаг "Deploy to VPS via SSH"
- Увидите логи деплоя

### На VPS (Docker):
```bash
docker-compose logs -f --tail=50 obsidian-bot
```

### На VPS (Screen):
```bash
screen -r obsidian-bot
```

---

## ⚠️ Важные примечания

### Безопасность:
- ✅ Приватный SSH ключ НЕ попадает в код
- ✅ Секреты хранятся зашифрованными в GitHub
- ✅ Доступ к деплою только у владельца репозитория

### Путь к проекту на VPS:
По умолчанию workflow ожидает проект в `/root/obsidian-telegram-bot`.  
Если у вас другой путь, измените в `.github/workflows/deploy.yml`:
```yaml
script: |
  cd /ваш/путь/к/проекту || exit 1
  ...
```

### Порт SSH:
Если используете нестандартный SSH порт (не 22), укажите его в секрете `VPS_PORT`.

---

## 📊 Что происходит при деплое?

1. **Trigger**: Push на ветку `main`
2. **GitHub Actions**: Запускает workflow
3. **SSH подключение**: GitHub подключается к VPS
4. **Git pull**: Подтягивает последние изменения
5. **Restart**: Перезапускает Docker контейнер
6. **Logs**: Показывает статус и логи
7. **Уведомление**: Вы видите результат в Actions

---

## 🎯 Готово!

Теперь при каждом push бот будет обновляться автоматически! 🚀

**Полезные команды:**

```bash
# Просмотр логов на VPS
docker-compose logs -f obsidian-bot

# Статус контейнера
docker-compose ps

# Перезапуск вручную
docker-compose restart

# Полное обновление (pull + rebuild)
./update.sh
```

---

**Дата:** 17.02.2026  
**Автор:** mkoval-pwork
