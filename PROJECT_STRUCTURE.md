# 📁 Структура проекта

## Основные файлы

```
ObsidianTelegramBot/
│
├── 🤖 Код бота
│   ├── bot.py              # Основной код бота (обработчики команд и сообщений)
│   ├── config.py           # Загрузка конфигурации из .env
│   └── github_handler.py   # Работа с GitHub API (создание файлов)
│
├── ⚙️ Конфигурация
│   ├── .env                # Токены и настройки (НЕ в Git!)
│   ├── .gitignore          # Файлы, игнорируемые Git
│   └── requirements.txt    # Python зависимости
│
├── 🚀 Запуск (Windows)
│   ├── START_BOT.bat       # ⭐ Главный скрипт запуска
│   └── STOP_BOT.bat        # Остановка бота
│
├── 🐧 Запуск (Linux/Mac)
│   └── start.sh            # Скрипт запуска для Unix
│
├── 🐳 Развертывание
│   ├── Dockerfile          # Docker образ
│   ├── docker-compose.yml  # Docker Compose конфигурация
│   └── obsidian-bot.service # Systemd сервис
│
└── 📖 Документация
    └── README.md           # Основная документация
```

## Файлы в корне vault

```
main vault/
│
├── setup_git.bat           # Настройка Git в vault
├── OBSIDIAN_GIT_SETUP.md   # Инструкция по Obsidian Git
└── WORKFLOW_RECOMMENDATIONS.md # Рекомендации по workflow
```

## Описание файлов

### Код бота

#### `bot.py`
Основной файл бота:
- Обработчики команд `/start`, `/help`
- Обработчик текстовых сообщений
- Проверка авторизации пользователя
- Логирование

#### `config.py`
Загрузка и валидация конфигурации:
- Чтение переменных из `.env`
- Проверка наличия обязательных параметров
- Экспорт настроек для других модулей

#### `github_handler.py`
Работа с GitHub:
- Подключение к репозиторию
- Создание файлов заметок
- Формирование YAML frontmatter
- Обработка ошибок GitHub API

### Конфигурация

#### `.env`
```env
TELEGRAM_BOT_TOKEN=your_bot_token
GITHUB_TOKEN=your_github_token
ALLOWED_USER_ID=your_telegram_id
GITHUB_REPO=username/repository
```

#### `requirements.txt`
```
aiogram==3.3.0
PyGithub==2.1.1
python-dotenv==1.0.0
```

### Запуск

#### `START_BOT.bat` (Windows)
- Проверяет наличие Python
- Устанавливает/обновляет зависимости
- Запускает бота
- Показывает статус

#### `STOP_BOT.bat` (Windows)
- Находит процесс бота
- Завершает его

#### `start.sh` (Linux/Mac)
- Создает venv
- Устанавливает зависимости
- Запускает бота

### Развертывание

#### `Dockerfile`
Docker образ на базе Python 3.10-slim

#### `docker-compose.yml`
Конфигурация для запуска через Docker Compose

#### `obsidian-bot.service`
Systemd юнит для автозапуска на Linux

## Что НЕ включено в Git

Файл `.gitignore` исключает:

```
.env                    # Токены и секреты
__pycache__/            # Python кэш
*.py[cod]               # Скомпилированные файлы
venv/                   # Виртуальное окружение
.vscode/                # IDE настройки
*.log                   # Логи
```

## Размеры файлов

| Файл | Размер | Описание |
|------|--------|----------|
| `bot.py` | ~5 KB | Основная логика |
| `github_handler.py` | ~3 KB | GitHub API |
| `config.py` | ~1 KB | Конфигурация |
| `README.md` | ~7 KB | Документация |
| `START_BOT.bat` | ~1.5 KB | Скрипт запуска |
| Остальные | <1 KB | Конфиги |

**Всего:** ~20 KB (без зависимостей)

## Зависимости

После установки `requirements.txt`:

- `aiogram` - ~700 KB - Telegram Bot API
- `PyGithub` - ~430 KB - GitHub API
- `python-dotenv` - ~20 KB - Переменные окружения
- + их зависимости (~5 MB)

**Итого:** ~6-7 MB установленных пакетов

## Workflow файлов

```
1. Сообщение в Telegram
   ↓
2. bot.py получает сообщение
   ↓
3. config.py предоставляет настройки
   ↓
4. github_handler.py создает файл
   ↓
5. GitHub сохраняет файл в 00_Inbox/
   ↓
6. Obsidian Git подтягивает изменения
   ↓
7. Файл появляется в Obsidian
```

## Логи

Бот логирует:
- ✅ Успешное сохранение заметок
- ⚠️ Попытки доступа от неавторизованных пользователей
- ❌ Ошибки GitHub API
- 📡 Статус подключения

Формат:
```
2026-02-16 22:38:52 - __main__ - INFO - Запуск бота...
2026-02-16 22:38:53 - __main__ - INFO - ✅ Подключение к GitHub успешно
2026-02-16 22:41:40 - __main__ - INFO - Получено сообщение от пользователя 203739654
2026-02-16 22:41:41 - __main__ - INFO - Заметка успешно сохранена
```

## Рекомендуемое использование

### Для разработки

```bash
python bot.py
```
Удобно для отладки, видны все логи в реальном времени.

### Для постоянной работы (Windows)

```
START_BOT.bat
```
Запуск двойным кликом, автоматическая установка зависимостей.

### Для сервера (Linux)

```bash
docker-compose up -d
```
Или systemd:
```bash
sudo systemctl start obsidian-bot
```

## Обновление

При обновлении кода:

1. Остановите бота (`STOP_BOT.bat` или `Ctrl+C`)
2. Обновите код (`git pull` или скачайте новую версию)
3. Запустите бота (`START_BOT.bat`)

Зависимости обновятся автоматически при запуске `START_BOT.bat`.

## Резервное копирование

Что нужно сохранить:
- ✅ `.env` - ваши токены и настройки
- ✅ Весь код (но он в Git)

Что НЕ нужно сохранять:
- ❌ `__pycache__/` - автоматически создается
- ❌ `venv/` - переустанавливается из `requirements.txt`

## Полезные команды

```bash
# Проверить статус бота (Windows)
tasklist | findstr python

# Просмотреть логи (если запущен в фоне)
Get-Content terminals/[id].txt -Tail 20

# Обновить зависимости
pip install -r requirements.txt --upgrade

# Проверить версию Python
python --version
```
